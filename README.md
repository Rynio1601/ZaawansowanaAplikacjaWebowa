
  # Generate matching app

  This is a code bundle for Generate matching app. The original project is available at https://www.figma.com/design/2bloMI1fuNDnUpQPB0wx82/Generate-matching-app.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  